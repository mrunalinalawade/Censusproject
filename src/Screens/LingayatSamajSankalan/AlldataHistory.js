import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const AlldataHistory = () => {
  return (
    <View>
      <Text>AlldataHistory</Text>
    </View>
  )
}

export default AlldataHistory

const styles = StyleSheet.create({})